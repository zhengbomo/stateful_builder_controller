name: stateful_builder_controller
description: control setState for StatefulWidget locally, optimize performance
version: 0.0.1-nullsafety.1
homepage: https://github.com/zhengbomo/stateful_builder_controller

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
